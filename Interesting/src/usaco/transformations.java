package usaco;

/*
ID: Max
LANG: JAVA
TASK: transformations
*/

import java.util.*;
import java.lang.Math;
import java.io.*;

public class transformations {
	
	static ArrayList<ArrayList<String>> before = new ArrayList<ArrayList<String>>();
	static ArrayList<ArrayList<String>> after = new ArrayList<ArrayList<String>>();

	public static void main(String[] args) throws IOException{
		// TODO Auto-generated method stub
		BufferedReader reader = new BufferedReader(new FileReader("Files/In"));
		PrintWriter writer = new PrintWriter(new BufferedWriter(new FileWriter("Files/Out")));
		int n = Integer.parseInt(reader.readLine());
		for (int i = 0; i < n; i++){
			
		}
		for (int i = 0; i < n; i++){
			
		}
		reader.close();
		writer.close();
		System.exit(0);
	}

}
